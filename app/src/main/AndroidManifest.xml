<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.farmer_facilitation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".change_password"/>
        <activity android:name=".change_username" />
        <activity android:name=".profile" />
        <activity android:name=".Single_notification" />
        <activity android:name=".Apply" />
        <activity android:name=".AddHarvest" />
        <activity android:name=".MyHarvest" />
        <activity android:name=".Notification" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity" />
        <activity android:name=".start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>